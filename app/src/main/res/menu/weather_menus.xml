<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
      xmlns:app="http://schemas.android.com/apk/res-auto"
    >

        <item android:id="@+id/adds" android:title="@string/menu_add" app:showAsAction="never|withText"></item>

        <item android:id="@+id/share" android:title="@string/menu_share" app:showAsAction="never|withText"></item>

        <item android:id="@+id/sets" android:title="@string/menu_set" app:showAsAction="never|withText"></item>

</menu>